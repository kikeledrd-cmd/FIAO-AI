# 2026-08-17

## Task 16 — Devoluciones/apartados y lealtad/promociones (completada)

Cerré la Task 16 del Plan 2 (Core Commerce). Todo el trabajo en `fiao/`.

### Qué se implementó

- **Fase A — Apartados** (`ProductStock.reserved`, disponible = onHand − reserved):
  - Dominio `packages/domain/src/apartado/apartado-policy.ts`.
  - Contratos `packages/contracts/src/apartado.ts`; migración `20260817003000_commerce_apartado_loyalty`.
  - Procesadores `process-apartado-create` (reserva + anticipo INJECTION a caja, exige sesión abierta), `process-apartado-complete` (venta real con `APARTADO_CREDIT` + resto), `process-apartado-cancel` (libera reserva, `APARTADO_REFUND` crédito a favor + retiro de caja, PIN del dueño).
  - `GET /api/apartados` + pantalla `/apartados` (crear/completar/cancelar).
- **Fase B — Lealtad + promos**:
  - `loyalty-policy.ts` (saldo computado con vencimiento; 1 punto por RD$1 con tasa demo 100), `promotion-policy.ts` (`applyPromotions` pura determinística).
  - `process-sale.ts` valida promos (recompute server-side, `PROMOTION_MISMATCH`), redención (`INVALID_REWARD`) y earn/redeem de puntos; `process-sale-reversal.ts` revierte puntos.
  - `GET /api/loyalty` (config + saldo por cliente), `/api/rewards`, `/api/promotions`; pantalla `/loyalty`.
  - POS: promos en vivo + payload `discountCents`/`promotionIds`; recibo muestra total pagado.
- **Sync/Dexie**: `APARTADO_CREATE/COMPLETE/CANCEL`, `reduceApartadoChange`/`reduceLoyaltyChange`, Dexie v6 (5 tablas), sync client ajusta reservas y aplica LOYALTY; `CatalogProduct.reserved`.
- **E2E** `apartado-loyalty.spec.ts` (4 tests).

### Verificación final

- `lint`: 0 errores (1 warning postcss preexistente).
- `typecheck`: limpio.
- `pnpm test`: **172** (unit + component).
- `pnpm test:integration`: **89**.
- `pnpm --filter @fiao/web build`: OK.
- `pnpm test:e2e`: **22/22** (2 auth + 2 sales + 3 credit + 3 inventario + 3 compras + 5 caja + 4 apartado/loyalty).

### Gotchas nuevos (importantes para el futuro)

- **`Sale.apartadoId` tiene FK `ON DELETE RESTRICT`** → el seed debe borrar `sale` ANTES que `apartado` (lo reordené).
- **`test:integration` trunca la base de desarrollo** (usa la misma DB en 5433) → **siempre re-seedear (`pnpm db:seed`) después de `test:integration` y antes de `test:e2e`**. Correr `test:e2e` varias veces seguidas sin reseedear acumula basura y produce flakiness.
- Amplié el enum de propósitos de `POST /api/owner/authorize` (`ownerAuthorizeRequestSchema` en `contracts/inventory.ts`) para incluir `PURCHASE`, `CASH_EXPENSE/WITHDRAWAL/INJECTION/CLOSE` y `APARTADO_CANCEL` (los procesadores ya los usaban; el endpoint solo admitía 2).
- El E2E asume credenciales seed `+18095550123/1234`; si el seed corre con `FIAO_SEED_*` personalizadas (p. ej. desde env global), los tests fallan en login. Los defaults viven en `prisma/seed.ts`.
- No usar `Remove-Item` (Safety Guard lo deniega). Para archivos temporales, moverlos a `.openclaw/tmp/` con `Move-Item`.

### Estado Git

- Cambios de Task 16 sin commitear todavía al redactar esta nota. Siguiente paso: commit + push.

## Contexto general

- Repo: `kikeledrd-cmd/FIAO-AI`, monorepo pnpm. Puerto Postgres 5433 (Docker `fiao-postgres-1`, image postgres:18).
- Plan 2 queda completo (POS, fiado, inventario, compras, caja, apartados/lealtad). Siguiente: **Plan 3 — Pedidos por WhatsApp + delivery básico**.
