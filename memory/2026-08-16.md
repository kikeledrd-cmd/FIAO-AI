# 2026-08-16 — FIAO: Task 11 POS Ventas completada

## Task 11 — POS ventas (Plan 2) ✅ commit `dd94485`

Implementación completa + verificación full + push a `main` (kikeledrd-cmd/FIAO-AI).

### Lo que se construyó
- **Contratos**: `packages/contracts/src/sales.ts` (Zod: saleQuantity, saleLine, salePayment, saleOperationPayload).
- **Dominio**: `packages/domain/src/sales/sale-policy.ts` — reglas puras (totales en centavos, cantidades decimales fijas, pagos mixtos validados).
- **Persistencia**: modelos `Product`/`ProductStock`/`StockMovement`/`Sale` + migración `commerce_sales`; `process-sale.ts` idempotente (append-only, REJECTED con errorCode, audit + syncChange); despacho en `process-operation.ts`.
- **Sync**: `SALE` en `ALL_OPERATION_TYPES`, `reduceSaleChange`, `GET /api/catalog` branch-scoped, tabla Dexie `catalog` v2.
- **UI**: `/sell` touch-first (búsqueda, carrito, cantidades, cobro efectivo/transferencia/tarjeta/mixto, recibo), card "Vender" en home, branding oficial.
- **Seed**: 10 productos demo por sucursal (20 total), incl. ítem sin stock (recarga).

### Bug importante encontrado y corregido
- El schema Zod del push (`apps/web/app/api/sync/push/handler.ts`) solo aceptaba `type: "NOOP"` — toda venta `SALE` sincronizada fallaba con `INVALID_REQUEST` (el cliente mostraba "Error 1" en el header). Fix: `z.enum(ALL_OPERATION_TYPES)`. **Lección: al añadir un tipo de operación nuevo, actualizar el schema del push handler.**

### Verificación (todo en esta máquina, Node 22 + PG18 Docker en 5433)
- `pnpm lint`: 0 errores (1 warning preexistente en postcss.config.mjs)
- `pnpm typecheck`: OK
- `pnpm test`: 63 (unit + componente; nuevo `sales-screen.test.tsx` con 5 tests, mocks con `vi.hoisted` para el catálogo)
- `pnpm test:integration`: 20
- `pnpm db:seed` + `pnpm --filter @fiao/web build`: OK
- `pnpm test:e2e`: 4/4 Chromium móvil (nuevo `sales-flow.spec.ts`: venta en efectivo + venta mixta offline→reconexión)

### Gotchas del E2E POS
- Botón del PaymentSheet cambia a "Guardar offline" cuando `navigator.onLine=false` — selector con regex `/Confirmar venta|Guardar offline/`.
- En modo offline, los `net::ERR_INTERNET_DISCONNECTED` en consola son esperados — filtrar en las aserciones del test.
- `page.reload()` justo tras `context.setOffline(false)` puede dar `net::ERR_ABORTED` (race del SW) — usar `waitForTimeout(800)` + `goto` con `waitUntil: "domcontentloaded"`.
- Para diagnóstico E2E: dump de IndexedDB crudo vía `page.evaluate` (sin imports de alias en evaluate; abrir la DB con `indexedDB.open("fiao-offline")`).
- El spinbutton "Efectivo" existe 2 veces en el modal mixto → usar `exact: true`.

### Docs actualizados
- `docs/AI_HANDOFF.md` (fecha corte 2026-08-16, Task 11 ✅ con notas, próxima Task 12 fiado/clientes)
- `README.md` (sección POS ventas, próxima tarea Task 12)
- `docs/superpowers/plans/2026-08-13-fiao-pos-sales.md` (steps 1-11 ✅ + notas)

## Estado
- HEAD `main`: `dd94485` (push OK `15641a9..dd94485`)
- Docker `fiao-postgres-1` healthy; DB seedada
- Siguiente: **Task 12 — Fiado y clientes** (clientes con crédito, ventas a fiado, FIAO Score, cobranza) — ofrecida al usuario, no iniciada

---

## Task 12 — Fiado y clientes ✅ commit `f725a45` (push `dd94485..f725a45`)

### Lo construido
- **Dominio**: `contracts/credit.ts` (customer/abono/movimientos Zod) + `domain/credit/credit-policy.ts`: `creditBalanceCents` (saldo Σ FIAO_SALE − Σ ABONO), `assertCreditLimit`, `assertAbonoValid`, `computeFiaoScore` (v1 explicable: base 100, penaliza tardíos), `parseMoneyCents`.
- **Persistencia**: `Customer` + `CreditMovement` (append-only) + migración `commerce_credit`. Procesadores idempotentes: `process-customer.ts` (CUSTOMER_UPSERT), `process-abono.ts` (ABONO), `process-sale.ts` extendido (FIADO: valida cliente + límite, crea cargo FIAO_SALE y delta CREDIT).
- **Sync**: `CUSTOMER_UPSERT`/`ABONO` en `ALL_OPERATION_TYPES`; `reduceCustomerChange`/`reduceCreditChange`; `GET /api/customers` branch-scoped; Dexie v3 (`customers` + `creditMovements`); sync-client aplica deltas CUSTOMER/CREDIT localmente.
- **UI**: `/customers` (lista con saldo, nuevo cliente, modal de abono); PaymentSheet con botón Fiado (puro/mixto) + selector de cliente con aviso de límite; card Clientes en home (href `/customers`).
- **Seed**: 3 clientes/sucursal (ids únicos por branch: `...-100000000001` Los Mina, `...-200000000001` Invivienda), Doña María con saldo inicial RD$800 (límite RD$1,000). Seed ahora **totalmente idempotente**: limpia historial comercial del dueño demo (syncChange→stockMovement→creditMovement→sale→auditEvent→clientOperation→customer→productStock→product) para que corridas E2E repetidas no acumulen.

### Bugs reales cazados por tests
1. `Sale.customerId`/`CreditMovement.customerId` FK apuntan a la **PK interna** de Customer, no al `customerId` público del payload → resolver PK en la transacción.
2. `process-abono.ts` consultaba movimientos por el id público → todo abono fallaba con `ABONO_EXCEEDS_BALANCE` falso.
3. `exactOptionalPropertyTypes`: `customerId: null` explícito falla → spread condicional `...(customerPkId ? { customerId } : {})`; `tx` tipado como `Parameters<Parameters<FiaoPrismaClient["$transaction"]>[0]>[0]`.

### Gotchas E2E
- `selectOption` con `{ label: regex }` no funciona → usar valor del option.
- El botón Confirmar queda disabled si el fiado excede el límite (correcto) → elegir producto/saldo que cuadre.
- **PowerShell `Set-Content` corrompe UTF-8** (Doña María → DoA�a) → reescribir specs E2E con la herramienta `write`.
- `customerIdForBranch` con suffix debe producir UUID válido (último grupo 12 hex chars): `40000000-0000-4000-8000-${branchNum}0000000${suffix}`.

### Verificación (todo en esta máquina)
- `pnpm lint` 0 errores (1 warning preexistente postcss) · `pnpm typecheck` OK
- `pnpm test` 84 · `pnpm test:integration` 30 · build OK · `pnpm test:e2e` **7/7** Chromium móvil (3 nuevos de credit-flow: fiado 800→825, abono 275→75, crear cliente).

### Docs
- `docs/superpowers/plans/2026-08-13-fiao-credit-customers.md` (steps 1-11 ✅ + notas)
- `AI_HANDOFF.md` (Task 12 ✅; plan activo credit-customers; próxima Task 13 inventario/reversos)
- `README.md` (sección fiado/clientes; próxima Task 13)

## Estado
- HEAD `main`: `f725a45`
- Siguiente: **Task 13 — Inventario y reversos** (ajustes de stock con autorización OWNER, devoluciones/anulaciones con trazabilidad) — no iniciada

---

## Task 13 — Inventario y reversos ✅ commit pendiente (verificación completa)

### Lo construido
- **Contratos** `contracts/inventory.ts`: `stockAdjustmentPayloadSchema`, `saleReversalPayloadSchema`, `ownerAuthorizeRequestSchema` (purpose enum).
- **Dominio** `domain/inventory/inventory-policy.ts`: `parseAdjustmentDelta` (con signo, normaliza, rechaza cero/4+ decimales) + `applyStockDelta` (nunca negativo, `STOCK_NEGATIVE`). Reutiliza `addDecimalQuantities`/`subtractDecimalQuantities`/`parseSaleQuantity` de sale-policy (fix: `parseSaleQuantity` rechaza "0" → check explícito de cero antes; `applyStockDelta("0", "5")` devuelve el delta directo).
- **Procesadores** `process-stock-adjustment.ts` + `process-sale-reversal.ts` + `shared.ts` (`persistRejectedOperation` reutilizable) + `isOwnerAuthorized` (rol OWNER pasa directo; CASHIER exige OwnerAuthorization con purpose + targetOperationId = operationId + no expirada). Reverso: valida venta del branch, no revertida (`SALE_ALREADY_REVERSED`), crea StockMovement `REVERSAL` +cantidad por línea y CreditMovement `REVERSAL` + delta CREDIT si fiado; emite syncChange `REVERSAL` `{reversalId, saleId, lines}`. `COMMERCE_OPERATION_TYPES` += `STOCK_ADJUSTMENT`, `SALE_REVERSAL`.
- **API** `POST /api/owner/authorize`: handler inyectable (repository/verifyPinHash/now/requireSession); valida sesión + acceso a branch, verifica PIN contra `findActiveOwnerAuthorizers` del dueño (cajero puede pedir autorización), emite OwnerAuthorization con `ownerAuthorizationExpiresAt` (TTL 5 min).
- **Sync** `reduceStockAdjustmentChange`/`reduceReversalChange`; `applySignedStockDeltas` (REVERSAL suma, ajuste con signo); sync client aplica ambos tras el pull.
- **UI**: `/inventory` (página con AppShell + force-dynamic, lista con stock + modal Ajustar con delta/motivo/PIN si cajero), card Inventario con href, botón **Anular venta** en recibo (modal motivo + PIN si cajero; restaura stock y saldo fiado local).

### Verificación
- lint 0 errores (1 warning preexistente postcss) · typecheck OK
- `pnpm test` **101** · `pnpm test:integration` **43** (30 previos + 13 nuevos)
- build OK (fix: página protegida sin AppShell → `APP_SHELL_REQUIRED` en prerender)
- `pnpm test:e2e` **10/10** (2 auth + 2 sales + 3 credit + 3 inventario) — inventario: ajuste +5 OWNER, reverso cash restaura stock (medición dinámica con `expect.poll`), cajero con PIN incorrecto → alert visible

### Gotchas nuevos
- `exactOptionalPropertyTypes`: pasar `{ ownerAuthorizationId: payload.ownerAuthorizationId ?? null }` y castear payloads JSON a `as never`.
- Los payloads mínimos de los reducers exigen los campos clave (adjustmentId+productId, reversalId+saleId).
- El selector del botón Ajustar en E2E: el nombre del producto en inventario es un `<strong>` dentro de `<li>` → `xpath=ancestor::li` + `getByRole(button, Ajustar)`.
- Los tests `.test.tsx` corren bajo `--project=component`, no `unit`.

## Estado
- HEAD `main`: `bbb660a` (push OK `f725a45..bbb660a`)
- Siguiente: **Task 14 — Compras y proveedores** (gestión de proveedores, compras y costo promedio móvil) — no iniciada

---

## Task 14 — Compras y proveedores ✅ commit pendiente (verificación completa)

### Lo construido
- **Dominio** `domain/purchasing/purchase-policy.ts`: `computeMovingAverageCost` determinístico (aritmética entera: cantidades en milésimas, costos en centavos, redondeo fijo half-away-from-zero; sin stock previo o costo 0 → la compra fija el costo) + `assertPurchaseLineValid`. 11/11 tests.
- **Persistencia** migración `20260816194608_commerce_purchasing`: `Supplier`, `Purchase`, `PurchaseLine` (append-only) + `Product.costCents`. Procesadores `process-supplier-upsert.ts` (datos maestros) y `process-purchase.ts` (exige OWNER o OwnerAuthorization purpose PURCHASE; StockMovement PURCHASE +qty; costo promedio). `SupplierRepository.listByBranch` con stats.
- **Sync**: `SUPPLIER_UPSERT`/`PURCHASE` en COMMERCE_OPERATION_TYPES; `reduceSupplierChange`/`reducePurchaseChange`; Dexie v4 (`suppliers`); sync client aplica PURCHASE (stock + costCents) y SUPPLIER.
- **UI**: `/suppliers` (lista + crear offline); modal **Registrar compra** en `/inventory` (proveedor opcional, líneas dinámicas, PIN si cajero); card Proveedores en home; `CatalogProduct.costCents`.
- **API**: `GET /api/suppliers` branch-scoped.

### Verificación
- lint 0 errores (1 warning preexistente postcss) · typecheck OK
- `pnpm test` **117** · `pnpm test:integration` **54** (43 + 11 nuevos)
- build OK · `pnpm test:e2e` **13/13** (2 auth + 2 sales + 3 credit + 3 inventario + 3 compras)

### Gotchas nuevos
- El seed fallaba tras la migración: `clientOperation` borraba antes que `purchase`/`purchaseLine`/`supplier` (FK Restrict nuevo) → reordenar la limpieza.
- `parseSaleQuantity` rechaza "0" → manejo explícito de cero en sumas de stock.
- Los tests de reducers con keys parciales (elipsis) fallan; usar ownerId+branchId completos.

## Estado
- HEAD `main`: pendiente commit Task 14
- Siguiente: **Task 15 — Caja** (apertura/cierre, gastos, retiros, inyecciones, arqueo con diferencias) — no iniciada
