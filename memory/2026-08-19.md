# 2026-08-19

## Task 18 — FIAO AI Orchestrator and Voice (Plan 4)

Completada y verificada. Cierre del Plan 4 del roadmap MVP V1.

### Qué se hizo

- **Dominio** `domain/ai/ai-intent.ts`: `parseAiIntent` determinístico (español/dominicano) que clasifica QUERY vs ACTION y extrae monto/customerQuery; matching por **palabra completa** (word boundary) para que "venta" no matchee dentro de "inventario"; `extractAmountCents` (RD$ / pesos / "con N"), `detectAnomalousAmount`, labels `CONFIRMED|ESTIMATED|RECOMMENDATION`. La ambigüedad de entidades nunca se auto-resuelve. 17 tests.
- **Contratos** `contracts/ai.ts` + **migración** `commerce_ai` (`AiAuditLog` append-only + `AiActionToken` de un solo uso con TTL).
- **Repos** `AiQueryRepository` (agregaciones read-only: ventas, fiado, saldo de cliente, inventario bajo, caja, pedidos; reutiliza `computeExpectedCashForSession`) y `AiAuditRepository` (log + tokens).
- **Tool registry** `database/ai/tools.ts`: `runAiQuery`, `buildActionPlan` (resuelve cliente por nombre con ambigüedad, ids estables para replay), `executeActionPlan` → `processOperation`. Acciones: abono y apertura de caja con confirmación; ajuste de stock protegido (PIN); venta/pedido → "hacerlo desde la UI" en V1.
- **Orquestador** `apps/web/lib/ai/orchestrator.ts` + `provider.ts` (adapter mockeable; el modelo SOLO produce intents). `handleMessage` + `confirmAction` con audit log.
- **API/UI**: `POST /api/ai/message`, `POST /api/ai/confirm`; pantalla `/ai` (chat + voz Web Speech + confirmación + PIN); card FIAO AI en home.

### Verificación

- `lint` 0 errores (1 warning postcss preexistente); `typecheck` limpio.
- `pnpm test` **208** (antes 192; +17 dominio AI).
- `pnpm test:integration` **107** (antes 100; +7 `orchestrator.integration.test.ts`).
- `build` OK (aparecen `/ai`, `/api/ai/message`, `/api/ai/confirm`).
- `test:e2e`: los 3 casos de `ai-orchestrator.spec.ts` pasan; suite completa **27/29** (2 flaky preexistentes offline: `auth-and-offline` y `sales-flow` con `context.setOffline()` + `page.reload()` → `net::ERR_FAILED`).

### Exit gates cumplidos

- El modelo no ejecuta mutaciones directamente (pasa por `processOperation` + confirmación).
- Cajero no obtiene datos owner-only (ninguna query expone margen/diferencia/contado).
- Nombres ambiguos nunca se auto-resuelven (devuelven opciones).
- Replay/idempotencia por token de un solo uso (segunda confirmación → "ya fue ejecutada").
- Suite de intents core en `ai-intent.test.ts` + `orchestrator.integration.test.ts`.

### Gotchas nuevos

- **Docker Desktop se apaga entre sesiones** (Postgres 5433 cayó con `ECONNREFUSED` a mitad de la tarea); reiniciar con `Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"` + `docker compose up -d` y esperar antes de testear.
- `AiActionToken.tokenId` se genera en el código (no tiene `@default` en Prisma); el `payload` se castea a `Prisma.InputJsonValue`.
- Los payloads usan el **`customerId` público**, pero `CreditMovement.customerId` es FK al `id` interno → resolver cliente antes de consultar saldo.
- Los overrides null del handler de API **no deben pisar** el monto extraído del intent (fusionar ignorando null); si no, "abona 500" se convertía en monto 0.
- El E2E de "abrir caja" chocaba con `CASH_SESSION_ALREADY_OPEN` (cash-flow.spec abre caja antes) → usar "abona X a <cliente>" para la acción con confirmación (no depende de estado global).

### Siguiente

**Plan 5 — Reportes, Onboarding, Configuración, Demo y Exportaciones** (pendiente de confirmación).

## Task 19 — Reportes, Onboarding, Configuración y Exportaciones (Plan 5)

Completada y verificada. Cierre del Plan 5 del roadmap MVP V1.

### Qué se hizo

- **Dominio** `domain/reports/report-policy.ts`: `startOfDay`, `previousPeriodStart`, `percentChange`, `estimatedProfitCents`, `profitLabel`, `stockLabel` (helpers puros) + `report-policy.test.ts`.
- **Contratos** `contracts/reports.ts` (tipos de reporte/dashboard + CSV) y `contracts/settings.ts` (business settings, onboarding, dispositivos).
- **Migración** `commerce_settings_reports`: `BusinessSettings` (upsert singleton por sucursal) y `OnboardingState` (milestones).
- **Repos** `report-repository.ts` (dashboard + 7 reportes + exports CSV; todo reconciliado desde ledgers append-only) y `settings-repository.ts` (Settings/Onboarding/Device; revocar invalida sesiones).
- **API**: `GET /api/reports`, `GET /api/reports/export`, `GET/PUT /api/settings`, `GET /api/onboarding`, `GET /api/devices`, `POST /api/devices/revoke` (DASHBOARD/PROFIT/PRODUCTS owner-only).
- **UI**: `/reportes` (tabs + export CSV), `/configuracion` (ajustes + dispositivos), banner de onboarding en home, cards Reportes/Configuración.

### Verificación

- `lint` 0 errores (1 warning postcss preexistente); `typecheck` limpio.
- `pnpm test` **220** (antes 208; +8 report-policy +4 csv).
- `pnpm test:integration` **122** (antes 107; +9 report-repository +6 settings-repository).
- `build` OK (aparecen `/reportes`, `/configuracion`, `/api/reports`, `/api/reports/export`, `/api/settings`, `/api/onboarding`, `/api/devices`, `/api/devices/revoke`).
- `test:e2e`: los 4 casos de `reports-settings.spec.ts` pasan; suite completa **32/33** (1 flaky preexistente offline `auth-and-offline` con `net::ERR_INTERNET_DISCONNECTED`).

### Gotchas nuevos

- Tras crear una migración hay que **`prisma generate`** antes de typecheck (el cliente no conocía los modelos nuevos).
- `CsvRow` solo acepta `string | number` → exportar booleanos como `0/1`.
- `Object.keys(rows[0])` exige guard de `noUncheckedIndexedAccess` (`CsvRow | undefined`).
- Con `exactOptionalPropertyTypes`, el `upsert` de settings debe construir update/create con **spreads condicionales** (los `undefined` de un `Partial` no se aceptan en `create`).

### Siguiente

**Plan 6 — Seguridad, Deployment y Piloto** (pendiente de confirmación del usuario).
