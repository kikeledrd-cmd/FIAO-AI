# 2026-08-20

## Task 20 — Security Hardening, Deployment y Pilot Readiness (Plan 6)

Completada y verificada. Cierre del Plan 6; el MVP V1 queda completo según el roadmap.

### Qué se hizo

- **Security headers** (`next.config.ts` `headers()`): nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy; en producción HSTS + CSP (`frame-ancestors 'none'`).
- **CSRF** (`apps/web/middleware.ts`, matcher `/api/:path*`): rechaza mutaciones cross-site por `Origin`/`Sec-Fetch-Site`; webhook de WhatsApp exento (HMAC-SHA256).
- **Lockout persistente** (migración `commerce_security_lockout`): `User.failedLoginAttempts`/`lockedUntil`, backoff exponencial en DB (5 libres → 30s ×2 hasta 15 min) + throttle en memoria.
- **Seed/demo separation** (`FIAO_SEED_MODE=demo|prod`): demo idempotente (bloqueado en prod); prod mínimo (dueño + sucursales + settings) con `FIAO_ALLOW_PROD_SEED=true`.
- **Observability + PII redaction**: `apps/web/lib/observability/logger.ts` (JSON-lines, `redactPii`) usado en login.
- **Pilot analytics**: `PilotEvent` (migración `commerce_pilot_events`) + `AnalyticsRepository`; `GET /api/analytics/summary` (owner-only) y `POST /api/analytics/event`; `USER_LOGIN` emitido server-side.
- **Runbooks**: `docs/runbooks/{sync-reconciliation, database-backup-restore, whatsapp-degraded-mode, ai-degraded-mode, pilot-onboarding-checklist, deployment}.md`.

### Verificación

- `lint` 0 errores (1 warning postcss preexistente); `typecheck` limpio.
- `pnpm test` **230** (antes 220; +5 middleware CSRF +3 PII +2 lockout).
- `pnpm test:integration` **131** (antes 122; +4 analytics +5 auth-isolation).
- `build` OK (middleware compilado; `/api/analytics/summary`, `/api/analytics/event`).
- `test:e2e` **32/33** (1 flaky preexistente offline `auth-and-offline`).

### Gotchas nuevos

- Tras `migrate dev` hay que `prisma generate` antes de typecheck (cliente desactualizado).
- `PilotEvent.metadata` con `exactOptionalPropertyTypes` → spread condicional.
- `z.record` requiere key+value (`z.record(z.string(), z.unknown())`).
- El middleware de Next corre en Edge: solo headers, no body ni DB.
- Docker Desktop se apagó otra vez entre sesiones → `Start-Process Docker Desktop.exe` + `docker compose up -d` + esperar `healthy`.

### Estado general

MVP V1 completo (Tasks 9–20, Planes 1–6). HEAD previo `d14a117`; commits de Task 20 a continuación. Siguiente: validación de piloto real con el checklist.
